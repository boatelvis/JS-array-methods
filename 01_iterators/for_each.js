const numbers = [5, 10, 15, 20, 25, 30, 35, 40, 45];

numbers.forEach((element, index) => {
    console.log(element);
    console.log(index);
})

const names = ["Eric", "Alfred", "Aikins", "Elvis"];

names.forEach((element, index) => {
    console.log(element);
    console.log(index);
})

const obj = [{town:"Tema"}, {town:"Accra"}, {town:"Sunyani"}];

obj.forEach((element, index) => {
    console.log(element);
    console.log(index);
})